import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Header from "@/components/header"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Dr. APJ Abdul Kalam - Tribute Website",
  description:
    "Educational and inspirational resource dedicated to Dr. APJ Abdul Kalam, the People's President and Missile Man of India",
  keywords: "APJ Abdul Kalam, India President, Missile Man, Scientist, Teacher, Inspiration",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
